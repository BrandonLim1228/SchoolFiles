void PrintMenu(void){
  Serial.println(F("a = test IR LED"));
  Serial.println(F("b = test switches"));
  Serial.println(F("c = test buttons"));
  Serial.println(F("d = test motor"));
  Serial.println(F("e = test motor and IR sensor"));
  Serial.println(F("f = test motor and encoder"));
  Serial.println(F("g = test motor and count stripes"));
  Serial.println(F("h = test motor and move to desired position"));
  Serial.println(F("i = test the solenoid and launch angle"));
  Serial.println(F("j = test the reloader"));
  Serial.println(F("k = demo 1"));
  Serial.println(F("s = get data from MATLAB"));
  Serial.println(F("t = Get data from MATLAB and compute angles"));
  Serial.println(F("v = demo 4"));
  Serial.println(F("z = print menu"));
  //Serial.println(F("done"));
  
}
